# 这版测过什么

当前源码为 **ContextOverlay 0.10.10 / API 2.2.0 / SDK 2.2.0 / schema 2**。开发与验收固定在 Windows、游戏 `1.126.73.1030`、简体中文环境。**0.10.9 已完成约 36 小时游戏时间的真实采集、正常退出和自动分层导出验收**；0.10.10 仅修正公共分页的完整编码预算，分类、总结、采集和导出实现不变。本次收尾未使用 Computer Use，编译包回放与游戏实际运行分别记录。

## 当前验收结论

| 范围 | 结果与边界 |
| --- | --- |
| 真实游戏正常结束 | 0.10.9 最后接收、落盘、session_end 和导出截点均为 21,197，无清理或写盘错误 |
| 游戏自动分层文件 | 27 份内容文件全部验证大小和 SHA-256；游戏自身导出耗时 12.813 秒 |
| 长日志默认预算 | 旧 0.10.8 的 190 MiB / 60 小时来源已由 0.10.9 编译包默认查询、导出和关闭后恢复验证 |
| 分页收尾修复 | 0.10.10 将完整返回字段和 JSON 分隔符计入 512 KiB 上限；原来只计算元数据和条目 |
| 自动化 | Python 3.7 全量 384 项通过，无跳过；包含完整页、单项、空页和精确预算边界 |
| 语义范围 | 对账通过不代表所有玩法已采集或所有分类已解析；高频未知资源仍保留证据 |
| 下游接入 | API/SDK 自动化和编译包回放通过；尚未完成真实下游 MOD 的游戏回调联调 |

## 2026-09-21：最新一局完整关闭（采集版本 0.10.9）

真实会话 `763e6884591745ab803fb3ec1ccaf458` 从周三 **20:30:17.440** 到周五 **08:30:04.080**，共 **35 小时 59 分 46.640 秒**。最终 **21,197 records = 20,935 次事件修订 + 262 条观察 → 10,190 latest events**。与暂停截点相比，只增加 71 条结束观察，全部最新事件逐字节不变。

`accepted_sequence = durable_sequence = session_end_sequence = export.source_sequence = 21197`；`capture_complete=true`、`journal_drained=true`，无 pending bytes、I/O 错误或 cleanup_errors。最终来源 **118,037,934 字节**，SHA-256 为 `9a4e502981d970b03dd3976f9ace5198db0c24b0f05dc86ae3f0ff2f2f1e6b94`。

游戏退出时自行生成 `views/snapshot-21197-67baeb61`，共 **27 份内容文件、71,274,698 字节**，耗时 **12,813 ms**。逐份核对文件长度和哈希；manifest 的来源序号、截点及哈希与最终 journal 一致，view-export-status 与 latest 也一致。四人物全部质量对账检查通过，阅读内容与暂停验收时相同，仅来源快照 ID 随最终截点变化。这里的文件确实由游戏中的 MOD 生成，没有用离线重导出代替退出结果。

| 人物 | records | events | organized | 正文阅读项 | 待核查动作 | recap 合计 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Eddie | 5,302 | 3,244 | 1,952 | 94 | 1 | 95 |
| Ava | 5,986 | 2,606 | 2,002 | 145 | 35 | 180 |
| Nyssa | 5,828 | 2,440 | 1,819 | 138 | 56 | 194 |
| Lila | 5,169 | 2,327 | 1,588 | 144 | 7 | 151 |

人物索引存在重叠，不能相加为全局计数。待核查动作也不包含所有非动作证据：Eddie 仍有 881 条待核查证据，其中 764 次内部数值变化明确关联 7 次小睡，属于真实重复操作而非重复写盘；后续改进见[未来方向](future.md)。

本机证据及复现脚本保存在 `.local/analysis/2026-09-21-premerge/`：`verify_game_close.py`、`game-close-verification.json`、游戏导出副本与四人物 Markdown。暂停状态下的四层分页、小时分布、名称、跨人物和具体语义检查保留于 `.local/analysis/2026-09-21-run-763e6/`。原始日志和游戏状态文件未改写。

## 2026-09-21：合并前审阅与分页边界修复（0.10.10）

以 `origin/main` 的 `23778ec` 为基线审阅 PR #10，重点检查持久化关闭、工作线程生命周期、内存回收、来源一致性和保守语义规则。确认的接口问题是完整页面可超过声明预算：修复前用 2,122 字节上限取得了 2,123 字节页面，因为身份、游标和数组分隔符没有计入。修复后在后台编码条目并计算完整返回字段大小，预留偏移数字空间后确定页面边界；单项或元数据无法容纳时明确返回 `view_budget`。取页仍只解码已确定的页面，没有增加游戏线程中的整局遍历。

新增回归覆盖 records、events、organized、recap 和 revisions 解释的完整编码大小与分页后内容一致性，以及空页／单项页恰好容纳和少 1 字节的失败。分页专项 **21 项通过**，Python 3.7 全量 **384 项通过，无跳过**。本次未调整资源映射、归并或摘要规则。

0.10.10 编译包另在本局最终日志上按默认预算完成四人物四层全量分页、缓存重读和六类解释，共检查 **578 页**；最大完整编码页 **524,144 字节**，均未超过 524,288 字节。修订链还原结果等于最新事件，organized 内容和顺序、recap 字段与游戏生成文件一致。证据为本机 `verify_compiled_pages.py`、`compiled-pages.json` 和 `tests.log`。这是新版编译包的独立进程回放，尚不是 0.10.10 的实际下游 MOD 回调联调。

已通过现有安装器备份 0.10.9 并部署 **0.10.10**；源码、构建 manifest 与本地安装哈希一致：`b2a62633a84759e84a18eaf54ed01c53533d6a0a2d3fb1b80a5d888e21ac692e`。没有制作分发 ZIP，也没有启动或关闭游戏。安装与哈希核验记录同存于本轮证据目录。

| 审阅项 | 结论与验证依据 |
| --- | --- |
| C1 正确性 | 来源序号／修订链、执行起止、角色与归并条件均有保守分支；完整分页预算问题已修复并复现验证 |
| C2 安全边界 | 本地同进程 MOD 接口；会话和条目身份校验、外部内容隔离、数字 Sim 导出路径及快照目录清理未发现新增问题 |
| C3 性能 | 原字节共享、解码缓存计费与回收、保守构建预留保持有效；4 GiB 为每个 store／导出任务的估算，不是游戏总 RSS |
| C4 错误恢复 | 只确认同步成功批次；停止接收后排空已接收记录；失败快照不替代成功结果，关闭状态与导出状态分开报告 |
| C5 测试 | 384 项通过；覆盖故障注入、取消、过期、低预算、来源损坏、完整页边界及不可变旧页 |
| C6 契约 | 原 API 语义保持兼容，分层能力通过 capability 声明；公共 snapshot_id 和 bundle 身份区分，完整页面预算与文档一致 |
| C7 可维护性 | API／SDK 版本和验收首页已同步；计划、debug 与下方旧验收明确标为历史；未删除原始证据 |

结构审计覆盖 **447 条基础资源映射、158 条活动规则、113 条名称释义**：身份无重复，角色与附带社交目标有效，版本一致，新增规则／释义的来源哈希字段齐备。这不等于重新独立审核了每条官方 tuning 或证明第三方覆盖兼容。

审阅结论：**已确认的分页问题修复后，未发现需要阻塞合并的剩余代码问题**。真实下游 MOD 回调联调、连续旅行新地块采集及更多玩法覆盖仍是明确的后续验收项。CodeRabbit 的 SUCCESS 是仓库星数门槛触发的跳过状态，不代表外部审阅通过；PR 仍需由维护者决定何时合并。

## 历史验证记录

以下保留各轮当时的版本、预算、计数和限制，供回归追溯；“当前”“未验收”“下一步”仅指各记录所在轮次。最新状态以上方结论为准。

### 2026-09-21：移除来源字节上限（0.10.9）

真实会话 `ec6cf833bb44453db0fae4d41b355486` 从周三 20:30:10.440 到周六 08:31:12.960，共 **60 小时 1 分 2.520 秒**。暂停截点 **36,347 records = 35,876 次事件修订 + 471 条观察 → 16,650 latest events**，199,457,340 字节（190.217 MiB），SHA-256 `860d0e5ea25bc02d642eac9076b765dc4c7192f3485227b45224d3424aec1da4`。原始写盘没有积压、错误或淘汰。旧版仍保留 128 MiB 来源上限，因此四层和导出均先返回 `view_budget`，尚未触及 4 GiB 内存预算。

新版从 runtime、API 构造、ViewStore 和 RunArtifacts 移除该独立字节门槛及参数。旧 config 中的 `event_view_source_mb` 被忽略，其他未知字段仍报错；metrics 不再提供 `source_budget_bytes`。完整来源仍逐条校验，来源字节及索引仍计入内存预算。记录数、单行、页面、构建时间和导出输出预算保留；分类／总结核心没有修改。

同一冻结前缀用 **0.10.9 编译包、全部默认预算**回放，RunArtifacts worker 按实际让步调度完成导出，耗时 **42.125 秒**，进程峰值工作集 **782,196,736 字节（约 746 MiB）**，全部 **27 份内容文件**大小／哈希正确。与上一轮 0.10.8 将来源上限临时提高至 256 MiB 的诊断导出相比，26 份内容文件逐字节相等；根 quality.json 仅诊断 coverage 描述不同，其余字段相同。该时间是独立 Python 3.7 进程测量，不是游戏主线程耗时，也不据此宣称本次删除门槛提升了算法性能。

四人分别在默认 ViewStore 保持四层打开并完成全量分页；修订还原、首次构建、缓存和导出内容／顺序一致：

| 人物 | records | events | organized | recap（含待核查动作） |
| --- | ---: | ---: | ---: | ---: |
| Eddie | 9,546 | 4,565 | 2,904 | 279 |
| Ava | 9,640 | 4,372 | 3,182 | 293 |
| Nyssa | 9,488 | 4,103 | 3,070 | 319 |
| Lila | 9,365 | 4,188 | 3,027 | 287 |

Python 3.7 全量 **382 项通过，无跳过**。新增回归实际写入超过 128 MiB 的连续修订历史，验证默认四层全分页及默认 worker 导出；另验证旧来源配置兼容而其他配置校验仍生效。原有低内存、取消、快照隔离、来源完整性与输出原子发布检查继续通过。

同一局随后退出，最终 **36,492 records = 35,876 次事件修订 + 616 条观察**，比暂停截点增加 145 条结束观察，16,650 个最新事件逐字节不变。严格关闭判据通过：`session_end`、accepted、durable 均为 36,492，`capture_complete=true`、`journal_drained=true`，没有清理或写盘错误。最终来源 199,622,687 字节，SHA-256 `fd291e17b7c97dcba53af7ee9e148d9327385eeb781a9b1e7d7cd4325c4eee1c`。旧版游戏退出导出实际返回来源上限错误；新版编译包在完整副本上用默认参数 **40.656 秒**补出全部 27 份文件，大小／哈希通过。四人人物回顾仅来源快照 ID 随最终截点变化，其他字段相等；各人的 records 比上表增加 2，其他层计数不变。这是旧版采集的离线恢复，不是新版游戏内退出测试。

安装器确认游戏进程已退出后备份 0.10.8 并安装 0.10.9；源码、构建及已安装脚本哈希相同：`bd92c5150db0ff7978d65b5a98e7c971da92b8a5638794d5b139c041e909cc49`。没有生成分发 ZIP。验收脚本与结果在本机 `.local/analysis/2026-09-21-no-source-limit/`。

### 2026-09-21：按预算复用解码结果（0.10.8）

0.10.7 为降低内存而反复解码，Eddie 单次构建在 9,619 个最新事件上调用解码 188,644 次。0.10.8 保留 ID 遍历、临时结果释放和省略无用扫描，同时在完整来源与原有构建预留之外，为解码缓存单独计费。预算足够时只解码一次，供各处理阶段和多人物复用；不足时不建立缓存。新来源加载、预算紧张或来源关闭时可以释放缓存，已发布页面不失效。

同一冻结前缀 `7141d0784687355bd1dcc283177ecd5c74022eb94f758fce6e6942cdc5919329`，在本轮独立 Windows Python 3.7 进程顺序测量：

| 测量 | 0.10.7 | 0.10.8 |
| --- | ---: | ---: |
| 四人物直接导出 | 35.5 秒 | 10.7 秒 |
| 峰值工作集 | 270,131,200 B（257.6 MiB） | 464,138,240 B（442.6 MiB） |
| 峰值提交量 | 266,149,888 B（253.8 MiB） | 459,743,232 B（438.4 MiB） |

直接导出耗时下降约 **70%**，以约 **185 MiB** 的额外峰值工作集换取多阶段复用。新版默认 4 GiB；上述内存来自 Windows 进程峰值 API，不是游戏总内存或 ViewStore 缓存估算。正常让步调度的编译包 worker 在本轮同源对照中从 **73.5 秒降至 20.8 秒**，27 份内容文件全部校验通过。测量时游戏已退出，不能将此耗时直接等同于游戏运行中的耗时。

编译包同一 ViewStore 依次构建 Nyssa、Ava、Eddie、Lila，累计解码始终为 **9,619 次**，缓存估算 **256,249,710 字节（244.4 MiB）**。首次请求含来源校验与缓存建立约 9.6 秒，随后三人约 3.4／3.3／4.2 秒；关闭 store 后缓存归零。四人物另行完成四层分页、首次／缓存／文件顺序、六类解释和非空名称审计。Eddie 四层保留时总缓存估算约 **378.3 MiB**，其中包含共享解码缓存。

0.10.7 与 0.10.8 的全部人物 bundle、原始最新事件与 organized 文件一致；分类、总结核心未变，快照身份也一致。历史五会话六截点的 24 个人物按新缓存路径逐字段通过。Python 3.7 全量 **380 项通过，无跳过**；新增覆盖精确缓存预算边界、多次构建不重复解码、取消释放、预算回退不影响旧页、新来源回收、关闭归零及两条导出路径所有内容文件逐字节相等。

本机证据位于 `.local/analysis/2026-09-21-adaptive-decode/`：`comparison.json`、`package-layers.json`、`shared-people.json`、`historical-replay.json`、`worker-export.json`。已部署 0.10.8，包 SHA-256：`5629c6fb9ee13e8162ddf84af4a418840555dd310c82231168e86f72e417c97a`；现有安装器备份 0.10.7 并校验替换结果。没有生成分发 ZIP。

同一局随后正常退出，最终 **20,940 records = 20,338 次事件修订 + 602 条观察**，比暂停截点增加 225 条结束观察；9,619 个最新事件逐字节不变。`validate_run.py --require-closed` 通过：accepted／durable／session_end 均为 20,940，`capture_complete=true`、`journal_drained=true`，无清理错误。完整来源 114,034,948 字节，SHA-256 `84e2ad4cbb9bb9216eb688689c8900f0a801c2226b8fad34bade35f330b7afae`。旧 0.10.6 的退出导出因 512 MiB 预算失败，原始写盘完整；新编译包在冻结副本上 **21.6 秒**重新导出成功，27 文件验证通过，人物内容与暂停截点相同（来源哈希及快照身份随截点改变）。证据为 `closed-validation.json`、`closed-export.json`，阅读副本为同目录 Eddie.md、Ava.md、Nyssa.md、Lila.md；未覆盖游戏自己的旧导出状态。这是离线补验，不是 0.10.8 游戏内退出验收。

### 2026-09-21：4 GiB 预算与按需解码（0.10.7）

默认 `event_view_memory_mb`、ViewStore 和 export_layers 均提高至 **4096 MiB**。这项限制是每个 store／导出任务的缓存与构建估算预算，按需使用；不是预分配 4 GiB，也不是整个游戏或 MOD 合计 RSS 上限。来源 128 MiB、100,000 条记录、512 KiB 页、120 秒构建及 256 MiB 导出限制继续独立生效。保守的 `source + 2 × latest_event_bytes` 构建预留未因实测较低而缩减。

内存优化包括可重复遍历的事件 ID 序列、按需解码、只排序键与 ID、决策样本只保留身份和摘录、独立结果深拷贝前释放全局构建器，以及逐人物导出后释放上一人的结果。合并索引扫描，经历构建只调用筛选规则，不计算未使用的完整筛选历史和字节指标。仍验证全部最新修订、处理跨人物依赖，公共返回值与来源隔离；未修改分类或总结规则。

session `0bdc09cb1a18458a96a0c947e42dd823` 的冻结前缀从周三 **20:29:44.120** 到周五 **13:00:22.760**，共 **40 小时 30 分 38.640 秒**；**20,715 records = 20,338 次事件修订 + 377 条观察 → 9,619 latest events**，113,782,359 字节。已接收与落盘序号相等，队列、inflight、错误均空；没有 session_end，仍按未结束片段导出。SHA-256：`7141d0784687355bd1dcc283177ecd5c74022eb94f758fce6e6942cdc5919329`。

旧版默认 512 MiB 时，导出保守估算需要 634,894,125 字节（约 605.5 MiB），进入构建前即失败。下面的性能比较采用独立 Windows Python 3.7 进程、同一冻结前缀、四个人物导出；旧包显式使用 4 GiB 以进入构建，新版使用默认值。由 Windows GetProcessMemoryInfo 读取进程峰值，不是 ViewStore 的缓存估算；宿主进程测量也不能当成游戏总内存或帧率测量。

| 测量 | 0.10.6 | 0.10.7 |
| --- | ---: | ---: |
| 峰值工作集 | 485,560,320 B（463.1 MiB） | 269,008,896 B（256.5 MiB） |
| 峰值提交量 | 482,279,424 B（459.9 MiB） | 265,826,304 B（253.5 MiB） |
| 直接导出耗时 | 16.6 秒 | 44.8 秒 |

峰值工作集减少约 **44.6%**，代价是重复解码增加耗时。编译包 RunArtifacts worker 按真实的让步调度及 120 秒预算运行，单独回放 **85.5 秒**通过；同时运行其他回归时 **98.7 秒**通过。27 份内容文件长度和哈希逐项核对，人物 bundle 与源码导出逐字节一致。时间结果有机器负载依赖，不承诺任意时长会话都能在限额内完成。

| 人物 | records | latest events | organized | recap（含待核查） |
| --- | ---: | ---: | ---: | ---: |
| Eddie | 6,343 | 3,119 | 2,126 | 187 |
| Ava | 5,407 | 2,525 | 1,743 | 173 |
| Nyssa | 4,697 | 2,170 | 1,657 | 156 |
| Lila | 5,593 | 2,410 | 2,021 | 339 |

以上数量与优化前一致。完整 events 和 organized 文件逐字节相同；bundle 仅排除实现哈希与派生快照 ID 后全字段相等，包括审计、时间边界、证据顺序和总结。历史六截点的 24 个人物同样逐字段对照；加上本局，合计六份会话、七截点、28 个人物视图。四人物分别通过编译包四层全量分页、首次／缓存／文件顺序比较、六类解释的重复读取及非空名称审计。Eddie 保留四层的缓存估算约 **133.9 MiB**，与上表进程峰值含义不同。

Python 3.7 全量 **374 项通过，无跳过**。新增测试覆盖遍历／排序／切片不保留解码对象、相同来源的哈希／大小／筛选／总结相等、结果修改隔离、取消和重复 ID 拒绝。源码、构建 manifest 与编译包一致；0.10.7 包 SHA-256 为 `25ebf968c5f727f099f19f24ed8792a527b7cebf48ce537f96dfaf2129f511ce`。现有安装器检测到 `TS4_x64` 后停止，已安装的仍为 0.10.6；游戏正常退出后才能部署。没有制作分发 ZIP，也没有 0.10.7 游戏内采集、MOD 回调或实际退出验收。

本机完整证据及脚本位于 `.local/analysis/2026-09-21-memory-4g/`：`comparison.json`、`historical-replay.json`、`package-layers.json`、`worker-export.json`，另有四人物阅读文件。原日志未修改；这些离线结果不会覆盖游戏自己的 views/latest.json。

### 2026-09-21：大日志分层查询、稳定顺序与户外活动（0.10.6）

Python 3.7 全量 **370 项通过，无跳过**，本轮新增 9 项回归。覆盖小预算下同时保留四层、源文件删除和返回值修改后旧页不变、编码页上限、十位以上引用的缓存／导出排序，以及烧烤成品、独立拿餐／进食／洗碗、选择入口、游泳／躺椅姿态、Join 指导、社交尝试与反应后果。

在同一个结束后的 91,007,661 字节来源上，0.10.5 先打开 Eddie 的 records/events 后占用估算 **163,348,690 字节**，再打开 organized 因构建预留超预算失败。0.10.6 共用来源原字节，前两层降至 **110,876,904 字节**，四层全部打开后为 **117,222,911 字节（约 111.8 MiB）**；默认 512 MiB 预算、构建预留和 512 KiB 页上限均未放宽。该值是缓存估算，不是进程 RSS 或实机卡顿测量。

本局同源 0.10.5 → 0.10.6 对照如下。正文包括活动、结果、关系和状态项；待核查仅指动作，不能据此声称全部来源已理解。四个人物的来源存在重叠。

| 人物 | records | latest events | organized 前 → 后 | 正文 前 → 后 | 待核查动作 前 → 后 | 名称问题 前 → 后 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Eddie | 7,742 | 3,018 | 2,361 → 2,358 | 86 → 99 | 38 → 0 | 23 → 14 |
| Ava | 4,133 | 1,806 | 1,328 → 1,308 | 102 → 118 | 39 → 2 | 17 → 4 |
| Nyssa | 3,940 | 1,778 | 1,288 → 1,244 | 110 → 127 | 49 → 0 | 31 → 9 |
| Lila | 3,837 | 1,662 | 1,231 → 1,218 | 87 → 97 | 27 → 1 | 14 → 8 |

54 个 tuning 的核对新增 54 条精确身份，含已验证的 `[Join]GroupCooking_Mentor`。三次烧烤接通实际制作父链和成品；两次游泳保留原执行开始，较早的姿态时间没有覆盖它。剩余 3 个人物视角中的待核查动作来自两个参与关系缺口；已解析资源但缺少参与证据仍保留待核查。名称缺口主要是聊天名称参数、物件或情感数值名，不填入推测文本。

五份真实会话的六个截点、24 个人物视图逐一检查来源哈希、每条已有证据的修订／执行事实、原活动的行动者、入队、开始、退出、结果与重要证据保留。新增上下文证据必须来自实体索引外的明确关联，不能当成新采集事件。四人物的完整分页与重新导出的 27 个文件一致；首次 organized、缓存命中及文件中的顺序和内容逐项相等，六类解释的重复读取一致。

独立 Python 3.7 进程只加载编译后的 **0.10.6 ts4script**，四人物按 records → events → organized → recap 顺序保留全部请求并完成全量分页；六类解释及非空名称审计通过，结果与源码导出一致。确认游戏已退出、源码与构建 manifest 一致后，现有安装器备份旧包并安装 **0.10.6**，部署哈希为 `9e7987e6fe4c549603b752b93ff8b7553e6267315d1284f0a177605559c79187`。没有制作分发 ZIP；尚无 0.10.6 新游戏运行或游戏内 MOD 回调验收。

### 2026-09-21：24 小时新一局正常结束（采集版本 0.10.5）

session `46e041ecd45e4f1292fd984a031a74fd` 从周三 **20:30:07.640** 到周四 **20:30:14.440**，共 **24 小时 6.8 秒**。正常退出后为 **18,226 records = 17,673 次事件修订 + 553 条观察 → 7,723 latest events**；相较退出前截点新增 228 条结束观察，最新事件逐字节不变。

`validate_run.py --require-closed` 通过，`capture_complete=true`、`journal_drained=true`，已接收／已落盘／session_end 序号均为 18,226，队列、inflight 和错误为空。游戏实际自动发布 `snapshot-18226-a885a4e4` 的 27 份内容文件长度及哈希全部验证通过。源 SHA-256：`106c9b915c6515385dfd679f47c6c8745ee6a9a343582a3526cba826f776a2c8`。这轮包含电影、洗澡、烧烤等样本；没有新增美食烹饪阶段或电脑游戏样本，不能补充它们的实机覆盖。

### 2026-09-21：美食烹饪、电影与附带聊天修复（0.10.5）

- Python 3.7 全量 **361 项通过，无跳过**，本轮新增 9 项回归。覆盖附带聊天的方向、角色、队列、决策、父引用、唯一实例、完整区间和版本边界，归并后的话题／后果／原始证据，以及美食阶段、独立进食和配方选择器的自主决策。
- 核对固定游戏 23 条 tuning，其中 18 条为新增审阅身份：电影覆盖同组全部 10 个资源，美食制作阶段接回烹饪链，节日／天气社交和扔饮料按真实执行状态表达。Nyssa 的卡普里三色沙拉从 **05:08:29 延续至 05:27:34**，包含 4 个阶段并关联成品；此前在 **05:14:15** 提前结束。后续吃饭保持独立。
- 官方 `sim_BeAffectionate` 指定在双方直接运行 `sim_Chat`。对这组精确资源与完整执行形状校验后，本局每个方向去掉一条重复根活动；Nyssa、Lila 各自阅读版都少两条，因为两人的视角都包含双方。原始交互没有删除，双方行动者没有互换。直接执行同样调用入队，所以同一 tick 的入队允许参与配对，提前或时间不明的独立排队不允许。

本局同源对照如下。正文包括活动、结果、关系和状态项，不等于互不重叠的经历数；人物之间来源重叠，不可相加作为全局事件数。

| 人物 | latest events（不变） | organized 前 → 后 | 正文项 前 → 后 | 待复核动作 前 → 后 | 名称问题（不变） |
| --- | ---: | ---: | ---: | ---: | ---: |
| Eddie | 2,456 | 2,193 → 2,192 | 46 → 46 | 1 → 0 | 0 |
| Ava | 674 | 496 → 484 | 31 → 33 | 6 → 1 | 1 |
| Nyssa | 777 | 574 → 559 | 55 → 56 | 5 → 0 | 4 |
| Lila | 547 | 387 → 384 | 31 → 31 | 4 → 0 | 0 |

剩余一条待复核动作来自 Ava 视角的“开始散播荒谬的谣言”：语义已知，但缺少该视角可确认的所属活动。其他未分类状态／数值仍在 review；上述数字只统计动作。现有聊天名称缺参仍明确显示，不按人物顺序补猜。

四份日志、十六个人物视图的来源对账通过。既有活动的角色、入队、开始、退出、结果及重要火灾证据保留；只将上述已核实附带聊天移出独立根活动。另一份历史日志中的同类聊天也按同一规则归并；最早 Eddie 的角色／时序回归保持通过。本局重新导出的 **27 份内容文件**长度和哈希全部通过，原 journal 与全局 latest events 字节不变。工作区证据位于 `.local/analysis/2026-09-21-followup-fix/`，原游戏自动输出单独保留。

独立 Python 3.7 进程仅导入编译后的 `.ts4script`，重建结果与源码导出一致。四层全量分页分别为 Eddie **7,181 → 2,456 → 2,192 → 46**、Nyssa **1,916 → 777 → 559 → 56**，六类解释入口均通过；Nyssa 的烹饪项回查到 20 个事件、47 次修订。这是宿主调用共用核心的检查，尚非游戏内 MOD 回调验收。源码与 manifest 一致；确认游戏已退出后通过现有安装器备份并部署 **0.10.5**，包 SHA-256 为 `d294897bb75c1ac0417999a4cd82b0ca7bc8a1ce25e7eee4f7b3285442ec1c64`。未制作分发 ZIP。

### 2026-09-21：用户新一局正常退出验收（采集版本 0.10.4）

session 为 `9eefe1a0567c460781a3f79b502a30e3`。采集时钟从周三 **20:29:44.120** 到周四 **08:01:22.800**，约 **11 小时 31 分 39 秒**；首个实际事件为 20:37:26，之前是 session_start 和范围观察。用户正常退出后，严格 `validate_run.py --require-closed` 验收通过。

- **11,095 records = 10,783 次事件修订 + 312 条观察，合成 4,070 latest events**；58,926,807 字节。源 SHA-256 为 `438805414a27d56a3bbd710823c43dbe6960224308c287dfaccf643b86189b5f`。
- `phase=closed`、`capture_complete=true`，已接收、已写盘及最终序号均为 **11,095**；写入队列、inflight 和错误均为空。相较退出前截点新增 133 条 scope_exit 和 1 条 session_end，最新逻辑事件保持逐字节一致。
- 游戏实际自动生成最终快照 `snapshot-11095-39cfa573`，**27 份内容文件**的长度／哈希与来源全部验证通过，导出耗时 **6.750 秒**。四人物的 306 条共享交互完成时序及角色一致性核查。
- 这是 0.10.4 的新运行和正常收尾证据；本局没有洗浴或新的电脑游戏样本，不作为这两类玩法的新增实机验收。记录与检查位于 `.local/analysis/2026-09-21-run-9eefe/`。

### 2026-09-21：暂停样本的语义修复（0.10.4）

- 用户更正本局开始时间为周二 **07:25**；日志从 **07:25:43** 到周三 **07:00:05**，此前基于“07:00 开始”的覆盖疑点撤销。截点含 **15,317 records / 7,622 latest events**，已接收和已持久化一致，队列为零。游戏未退出，未将有效前缀当作完整关闭验收。
- 全量 **352 项测试通过，无跳过**。新回归覆盖洗浴支持姿态、唯一候选、不同人物／物件／到访／时间、未结束和冲突父引用；新活动变体及 provider 阶段关联；烹饪内部火灾概率步骤不当作火灾；目标名称缺参及其审计。
- 固定游戏 tuning 核对后新增 48 条资源规则、修正洗浴姿态分类；补充和修正 15 条显示释义。Eddie 的同次洗浴从两项改为一项，原始两个交互及修订完整保留；后半夜小睡、电影、游戏以及暂停时仍在执行的行为进入正文。
- 当前四人对账：Eddie 正文 **93→104**、待复核动作 **25→0**、名称问题单元 **16→7**；Ava **100→108 / 32→1 / 26→7**；Nyssa **83→90 / 39→2 / 26→5**；Lila **114→127 / 26→2 / 11→6**。剩余名称缺参仍显式标注，未知 runtime 类和未核实的 Join 变体不强行归类。
- 对本局及 16,410 records 的历史局分别重新导出并验证 27 份内容文件，源 journal 和 latest events 保持一致；三份日志合计十二个人物视图的原有活动角色、入队、开始、退出及重要火灾证据保留。历史 Ava 的另外三处洗浴重复一并消除。最早 Eddie 的 r4、r11–r13、r18/r19、r53/r54 对应稳定单元的角色和时序回归通过。
- 本轮产物位于 `.local/analysis/2026-09-21-semantic-fix/`，包含前后比较、中文阅读版、tuning 证据、检查记录和脚本。正文增加及待复核减少是规则变更结果，不作为语义准确率。此修复当时仅做离线重放；随后 0.10.4 的新运行及正常退出验收见上节。

### 2026-09-21：统一离线和 MOD 来源校验（0.10.3）

修复离线生成与完整关闭验收会放过修订缺口的问题。`read_journal` 与 MOD 的 `read_prefix` 现在共用逐条校验，检查序号、会话、从 1 开始的连续修订、重复序号内容、记录类型及完整行；非法数字（包括浮点溢出）被拒绝。合法重试允许 JSON 空白及键顺序差异。FIFO 只影响返回的事件集合，不影响已见修订的校验。

- Python 3.7 全量 **344 项通过，无跳过**。新增回归覆盖跨入口拒绝行为、同尺寸修订损坏、严格结束 CLI、旧输出不被覆盖、合法重试、FIFO 淘汰后的修订链、截断／超长行和非法数字；修正原有测试夹具中不符合真实日志格式的初始修订、实体索引与结尾换行。
- 从真实 Journal/Recorder 生成正常关闭样本，只把第二次修订从 2 改为 3，保持文件长度和最终 run-status 不变：`integrity_passed=false`、`capture_complete=false`，`validate_run.py --require-closed` 返回 1，离线生成拒绝覆盖已有 bundle。
- 两份历史源分别为 **16,410 records / 7,647 events** 和 **14,954 records / 6,775 events**。离线与 MOD 读取的最终事件逐字段相同，输入 SHA-256 分别仍为 `dd443d674ff3c3a54350377707beb11c6c8d487a17a672e1eda17a89ea48afc7`、`c57e7c636dd3b594e2f1664b3df45502be9662b6ee0654301654da6be3b2c0fc`；原完整实机运行仍通过关闭验收。
- 第一份历史源的 Eddie、Ava、Nyssa、Lila 的详情 bundle、recap JSON、Markdown 和质量报告与 0.10.2 输出相同，分类／归并规则未变。
- 独立进程只从 `.ts4script` 导入，验证新校验器拒绝修订缺口，并重建 Eddie 的上述输出。源码 manifest 与包哈希一致，通过现有安装器完成旧版备份和安装后校验。包 SHA-256：`98b74fe9fc4ba9ce411af37bd8314ef36441915066ff9556643ad134558d93de`。

验证脚本和结果保存在工作区 `.local/analysis/2026-09-21-journal-validation/`。本轮未生成分发 ZIP；版本自动识别按固定游戏版本开发的约定暂不实现，约定见[开发与调试](development.md)。

### 2026-09-18：重要事件、活动归并与来源对账（0.10.2）

新增 38 条经安装游戏 tuning 核对的精确身份规则，覆盖火灾、阅读、电视／电影、洗澡、国际象棋、演奏与烹饪阶段。执行事实与分类／名称质量分别表达；已知但缺少所属活动的阶段保留详情，因产生后果而受保护的执行细节不会被当作未知语义或直接删去。待核查动作按原因分组阅读，所有执行实例及引用仍独立。新增自动 quality JSON／Markdown 和同源规则比较，见[对账协议与命令](event-quality.md)。

使用 0.10.1 完整实机日志的冻结副本重放：16,410 条原始记录、7,647 个最新事件，SHA-256 仍为 `dd443d674ff3c3a54350377707beb11c6c8d487a17a672e1eda17a89ea48afc7`。原实机快照和 journal 没有被改写。离线生成 27 份内容文件，全部长度／哈希、bundle 身份、原始来源对账通过。一次四人物离线生成耗时 8.719 秒；这不是游戏内耗时或帧率测试。

| 人物 | 最新事件（不变） | organized 前 → 后 | 正文阅读项 前 → 后 | 待核查动作 前 → 后 | 新版阅读分组 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Eddie | 2,011 | 1,400 → 1,327 | 108 → 116 | 87 → 20 | 16 |
| Ava | 1,772 | 1,478 → 1,401 | 98 → 108 | 79 → 25 | 15 |
| Nyssa | 2,087 | 1,851 → 1,776 | 104 → 119 | 101 → 35 | 18 |
| Lila | 1,613 | 1,245 → 1,236 | 113 → 128 | 50 → 26 | 16 |

正文阅读项包括活动、结果、关系观察和状态组，不等于互不重叠的经历数。待核查动作只计 interaction；其他未分类状态／数值仍在 review，并在 quality 中单独统计，不能把 Eddie 的 20 个动作理解成全部只剩 20 条问题。人物来源会重叠，不能合计成全局事件数。

- 64 个阅读动作、28 个电影观看子动作、10 个着火持续动作、8 个洗澡循环和 6 个下棋阶段，均通过记录中的实例关联归入所属活动，没有按时间猜测合并。副作用和因果证据仍可回查。
- Ava 的着火经历进入正文；灭火记录仍标为“未见开始／被替换”，没有写成已经灭火。其他人物的火灾反应、惊慌和撤离尝试分别保留。确认参与的重要火灾动作正文保留检查通过。
- 已有活动根实例、行动者、入队、开始和退出字段保持不变。Ava 一段烹饪的结束从 07:49 延至 07:57，依据新增识别的炉灶制作阶段及 parent_event_id；没有用后续无关动作补齐时间。
- 第二份历史源为 `99a3d4b1597743fb83c303f01a293b2f`，SHA-256 `c57e7c636dd3b594e2f1664b3df45502be9662b6ee0654301654da6be3b2c0fc`。四人来源对账通过；旧 Eddie r18/r19 的不同行动者和入队时间、r4/r11–r13/r26 的名称处理，以及 r53/r54 的未开始／入队／执行间隔按稳定单元 ID 回归通过。
- Python 3.7 全量 **339 项通过，无跳过**。覆盖重要事件执行不确定性、跨人物／实例／到访／对象／时间的错误归并、歧义 provider、各问题原因、分组引用守恒、同源比较、篡改拒绝、JSON 字段重新排序的确定性、质量报告失败不发布快照、SDK／接口兼容及临时分发内容检查。
- 隔离进程仅从 `.ts4script` 导入，使用导出的 events JSONL 重建 Eddie bundle、recap 和质量报告，与源码结果一致；修复了 JSON 字段顺序改变导致文字／名称审计顺序漂移的问题。10 个核心文件哈希与 39 个源码／资源文件的构建 manifest 核验通过。
- 已通过现有安装器部署 **0.10.2**，完成旧版备份和安装后哈希检查。包 SHA-256：`18a4923d9d8f2bfc8d1d87a964fc9fa27ba4bfadc69e28907c971014c3527c2d`。未制作项目分发 ZIP。

完整离线结果、各人物比较报告与验证脚本保存在工作区 `.local/analysis/2026-09-18-semantic-quality/`。仍有未分类资源和未关联事件；精确规则不能证明第三方 MOD 没有覆盖同一 tuning。对账用于发现缺失与处理变化，不是采集召回率或语义准确率。

### 2026-09-18：持续写盘与自动分层输出

用户上一局运行至次日 07:00，但 journal 最后只到当天 18:04:38；8,398 条记录构成合法前缀，缺少 session_end，不能认定整局完整。代码复现确认：队列拒绝入队会设置共享错误，旧写入线程随之退出，连已接受的数据也不再排空；Recorder 已暂停又使 Runtime 跳过首次错误日志。修复将入队拒绝与真实 I/O 故障分开，批量同步、继续排空已接受数据、保留独立诊断，并提供严格结束验收。**历史日志没有保存具体触发原因，不能反推那一局一定由队列满引起；缺失的历史部分无法恢复。**

自动文件使用与公开查询相同的来源校验和处理核心，包含全局最新事件、每位家庭成员的 organized、recap JSON／Markdown、详情 bundle 和文件哈希清单。最终快照只在全部文件成功后发布；中途导出明确标为片段。位置、预算与命令见[运行输出说明](run-output.md)。

验收使用原存档副本及原有伴随 MOD，家庭为 Eddie、Ava、Nyssa、Lila Landry；处理了剧情弹窗，主要使用三倍速，没有通过驱动注入需求数值、Buff 或指定活动。Computer Use 观察了起点、下午、夜间、次日凌晨和结束画面，并通过正常退出流程结束。不是连续录像，也没有逐项观察所有人物动作。

- session：`fd6d873a30a34b96bfe710ac358258a5`。游戏时间从 **day 2 07:25:43.240** 到 **day 3 07:01:50.560**，越过原先缺失的后半夜。
- **16,410 条原始记录 = 15,870 条事件修订 + 540 条观察记录**，合成 **7,647 个最新事件**；日志 79,161,099 字节。序号连续、修订链连续，源 SHA-256 为 `dd443d674ff3c3a54350377707beb11c6c8d487a17a672e1eda17a89ea48afc7`。
- `accepted_sequence = durable_sequence = session_end_sequence = 16410`，pending_bytes 为 0；run-status 为 closed、capture_complete=true，严格 `validate_run.py --require-closed` 通过。无采集、写盘或清理错误，无 FIFO 淘汰，没有新增游戏异常文件。
- 队列峰值 **146 / 2048 条**，待写内存峰值 **353,150 字节**；16,410 条记录用 **2,510 批**同步。独立压力回放将用户原来的 8,398 条记录按记录时序四倍速送入，并给每次同步额外增加 5 毫秒延迟：全部落盘、逐条一致，队列峰值 129，559 批同步。
- 中途导出截至 sequence **12079**，54.375 秒完成；最终自动导出截至 **16410**，**9.735 秒**完成。最终 18 份内容文件的长度、SHA-256、来源哈希、bundle 身份及层间对账通过，中途快照未被后续追加改变。
- Python 3.7 最终全量 **329 项通过，无跳过**。新增回归覆盖入队失败后排空、批次确认时机与顺序、I/O 故障诊断、原子快照发布、正常关闭导出及结束状态字段不一致时拒绝认证。独立隔离进程仅从当前 `.ts4script` 导入，以本轮整份日志重建 Eddie 四份文件，与游戏自动输出逐字节一致；源码 manifest 和包 SHA-256 核验通过。

最终人物结果如下。records 包括关联事件的全部修订、相关观察和共享边界；人物之间可能共享来源，不能把各行相加当全局总数。organized 还包含关联依赖；不同列的计数单位不同，差值不是删除的事件数。

| 人物 | records | events | organized | recap 主内容 | recap 待核查 | recap 总项数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Eddie | 3,935 | 2,011 | 1,400 | 108 | 87 | 195 |
| Ava | 4,066 | 1,772 | 1,478 | 98 | 79 | 177 |
| Nyssa | 5,003 | 2,087 | 1,851 | 104 | 101 | 205 |
| Lila | 3,319 | 1,613 | 1,245 | 113 | 50 | 163 |

待核查行动仍明确列出，不能把 recap 总项数都称为已确认的高价值经历。本轮处理持久化和文件可用性，没有扩展这些新玩法的分类规则。

游戏进行中的公共 API 验收固定在 sequence **4447**：Eddie **1,098 records → 584 events → 451 organized → 70 recap**。完整分页、最新修订对账、六种解释入口、旧页稳定、新来源推进到 10762、关闭一个请求后共享来源仍可用、非法 page_size 和旧 session 的明确错误均通过。首次来源构建 37.110 秒，同源 events 0.110 秒、organized 0.984 秒；recap 复用缓存。记录到的分页调用最大 4.894 毫秒，手动 export_views 的启动调用 0.655 毫秒，生成在后台完成。

同一地块采样 Zone.update 墙钟间隔如下，每段约 20 秒；游戏活动与速度并非严格受控，不能据此得出后台构建改善或不影响帧率的结论。这也不是渲染 FPS。

| 阶段 | 样本 | p50 ms | p95 ms | p99 ms | max ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 未请求视图 | 498 | 34.508 | 90.003 | 133.604 | 182.373 |
| 首次来源构建 | 558 | 34.107 | 52.381 | 104.134 | 132.601 |
| 共享来源分页／组织 | 531 | 34.122 | 66.075 | 113.346 | 144.008 |

退出后恢复 **32 个原存档、33 个 MOD 文件、Options.ini 和 config.json**，文件集合与哈希一致；开发驱动恢复原配置，测试副本清理，现有安装器确认 0.10.1 包与当前源码一致。本轮没有制作分发 ZIP。

证据保存在工作区 `.local/analysis/2026-09-18-live-persistence/`，完整运行及两份快照保留在游戏用户目录的对应 session 中。本轮未新增跨旅行、重新读档和所有玩法的实机验证；这些边界的自动化测试及旧版旅行证据不能替代新版本逐场景验收。大日志仍受明确的来源、内存和时间预算约束，超限报告失败，不截断后称为完整。

### 2026-09-18：游戏内分层接口

已实现 records/events/organized/recap 的异步请求、状态、分页、关闭／取消和同源解释。原历史接口默认行为不变，离线工具与运行时共用处理核心。当前契约见[分层查询文档](event-views.md)。

- Python 3.7 最终全量 **323 项通过**，其中 **9 项分层专项**。覆盖修订链、同源分页稳定性、原文件移除后旧页、损坏日志、冲突重试、取消共享构建、TTL／预算、历史 ID 不访问游戏实例、跨 session／线程拒绝、旅行期间关闭以及采样 Hook 清理。补齐只有范围观察、没有逻辑事件时的六类解释回查；SDK 新示例纳入导入验证，验收文档的 6 段 PowerShell 示例通过语法解析。
- Eddie 新接口：records **3,843 项（3,806 条修订＋37 条辅助记录）**，events **1,707 项**，organized **1,166 项（469 单元＋697 独立来源）**，recap **118 项**。r54 可回查实例 5941 的 5 次修订，保持入队与执行时间区别。
- Eddie/Ava/Nyssa/Lila 的 recap 分别 **118/135/130/129 项**，活动仍为 **62/85/71/79 项**。除快照身份外，全部字段与用户认可的旧离线版一致；四人共享同一个固定来源。
- 独立 Python 3.7 进程仅从 `.ts4script` 字节码包导入，规则资源加载和 Eddie 完整 recap 构建通过，结果与旧版逐字段一致。构建／安装器核对当前源码、规则资源及包哈希，通过旧版备份和安装后哈希验证；未制作新的分发 ZIP。
- 大日志负载使用约 82 MB 的固定 Eddie 会话。四层同时打开的缓存估算约 132 MB；四人物仅打开 recap、共享来源时约 97 MB。冷构建仍有 GIL／垃圾回收调度停顿，离线探针不能作为游戏帧时间结论。

**0.10.0 这一轮的实机状态：当时未验收。** 早先的启动尝试未成功，临时测试环境恢复并核验了原存档。后续 0.10.1 的新实机结果见上节；保留此处的离线基线，不把不同轮次的证据混在一起。

详细对照保存在工作区 `.local/analysis/2026-09-18-event-views/`，含 Eddie 分层对账、四人物结果和负载观察；该目录不进入分发包。

### 0.9.0 历史验证记录

- Python 3.7 全量测试 **304 项通过，无跳过**。覆盖 JSON 读写、去重、来源筛选、分页、增量、FIFO、旅行与读档边界、异常清理、SDK 示例、试用包完整性及离线筛选／经历组织／短版回查。
- 实机基础自检 **9/9 通过**，落盘检查通过，用户确认外部记录详情窗口正常。自检直接用 `co.api_inspect` 打开关联人物，避免误看其他 Sim 的历史。
- 实机一次旅行检查 **5/5 通过**：关联与无实体外部记录、checkpoint 续读、去重和取样的游戏历史都保留。旅行前后 session 相同，`zone_visit` 从 1 增至 2。
- 对这次旅行日志独立检查：204 条日志序号连续，35 个事件能完整读回；两条自检事件均只有修订 1，没有重复写入或改写记录地点。模块与窗口没有报错，游戏目录也没有这一轮的新异常文件。
- 本次整理补齐快速接入、统一示例导入路径，并明确了页面过期后保留 checkpoint 重读的用法。游戏脚本仍使用刚实测过的 0.9.0，不需要为了文档和 SDK 示例重新安装。

本机 2026-09-18 实机准备时校验出旧 manifest 与当前源码不符，已按当前源码重建 `.ts4script`；SHA-256：`61cb1e03f25a729a4c6b2d42d9cc15e7294a1e880f1d3ecf6d2766ebbf072595`。实际安装信息看游戏用户目录的 `ContextOverlay/install-receipt.json`。本轮没有制作 ZIP。

### 目前已有的证据

| 范围 | 跑过什么 |
| --- | --- |
| 当前状态、事件和中文文本 | 限定做饭、吃饭、社交场景的读取、历史和导出 |
| 游戏窗口 | 菜单、文字列表、正文详情，以及外部记录自检入口 |
| 常见事件 | 数值变化、Buff / 情绪、知识、特征、技能、制作、付款、库存、广播和反应的实机样本 |
| 资源说明 | 实机样本中 117 处 Buff 描述、25 项 Context 资源说明成功解析 |
| Autonomy 决策 | 一份实机快照含 1,162 条关联交互的决策，2,886 层候选通过数量与概率合计检查 |
| 读写和旅行历史 | 真实游戏自检、窗口确认，以及真实 Runtime / Journal 配合服务替身的生命周期测试 |
| 大量历史和资源限制 | 合成样本覆盖 200,000 条数量上限、内存预算、淘汰、查询冻结和持久化错误 |

这些范围里的样本通过，不等于全部玩法或资料片组合都覆盖了。具体采集范围看[架构说明](architecture.md)。

### 接下来还需要大家帮忙试

- **旅行后的持续采集**：这次到新地块后没有新增事件样本，只验证了旧历史仍能读到。往返、读档和 `co.restart` 隔离已有自动化测试，还可以补实机场景。
- **真正的下游接入**：不同 MOD 的加载顺序、游戏线程回调、后台模型结果回写，以及长时间运行。
- **复杂玩法**：职业、人生节点、Sentiment、库存拆堆与隐藏移动还缺实机样本；Autonomy 的立即执行、缓存和并行交互也需要继续试。
- **文本细节**：部分代词、姓名前缀、SIM_LIST、年龄条件和日期金额格式尚未解析。未知名称会保留原标记，资源说明也不代表客户端条件已经生效。
- **真实负载**：200,000 是数量上限，较重事件可能先碰到 1536 MiB 内存预算；来源筛选和“主要事件”也还不能代替经历总结。

一次查询显示 `complete` 只说明这次读取完成，不代表全部历史都还在、所有字段都有值或所有事件都已落盘。遇到问题按[快速接入里的反馈方式](quickstart.md#帮我们试什么怎么反馈)给出相关信息即可。

### 性能参考

本机 Python 3.7 上，10,000 条轻量合成外部事件的写入中位耗时约 0.055 ms、P95 约 0.084 ms；无变化增量查询中位约 0.071 ms。这个测量不含真实游戏和磁盘 fsync，不能直接当成游戏中的耗时。重现工具见[开发说明](development.md)。

### 离线经历视图试验

2026-09-17 的同一局日志包含 6,775 个最新事件；[离线经历视图](development.md#试验中的离线经历视图)当前版本为 `experience_view_v1_1`。在 Eddie 首版基础上检查另三位人物的原始行动和结果，再核对安装游戏的 224 个 tuning 资源，新增 188 条精确映射。未知内容仍保留待核查。

| 人物 | 原始关联事件 | 首版待核查 → 当前 | 当前摘要 token |
| --- | ---: | ---: | ---: |
| Eddie | 1,707 | 51 → 51 | 52,664 |
| Ava | 1,635 | 310 → 61 | 59,290 |
| Nyssa | 1,657 | 428 → 98 | 57,265 |
| Lila | 1,560 | 416 → 77 | 55,154 |

token 使用 `tiktoken 0.12.0 / o200k_base` 对紧凑 JSON 实测，不包含审计、详情、待核查内容及提示词包装。补回电影、吉他、争论、洗盘子等活动后，三人的摘要比首版更大；这次优先修复遗漏，没有把体积降低当作质量验收标准。Eddie 原始数据 2,925,548 token，当前摘要减少约 98.2%。输出仍有聊天容器，尚未形成很短的日终故事。

Python 3.7 全量 290 项测试通过，其中经历组织专项 28 项。原有 Eddie 75 条人工证据均可回查，74 条进入摘要；另三人的 33 条固定经历／知识方向检查、10 项结果及产物关联检查通过，包括 Ava 中断后恢复制作沙拉、产生怕失败特征、Nyssa 两次踢垃圾桶、Lila 弹吉他以及次日尚无成品的烹饪。它们是已选样本的验收，不是全量经历召回率。

另在复制存档上使用 computer use 观察一轮实机，session `e7d578b43d494caa9bff3afbd2e4154d`。在 08:27 的已落盘检查点保留 552 个最新事件、4 张观察画面，9 项对照检查通过：Lila 07:40–08:06 弹吉他后主动取消；暂停时提交又取消的研究没有开始记录；Ava 与 Nyssa 共用同一电视却有各自起止，Nyssa 的活动在 08:06 结束而 Ava 的活动仍未结束；Eddie 的自主电脑活动保持独立。完整运行日志及两份导出通过完整性检查，无错误、无淘汰。

这轮主动安排了吉他和电影、取消请求、控制暂停，并处理了存档开场提示；没有注入需求、Buff、关系分数或金钱。画面是离散观察，不是连续录像；精确时间来自日志。测试结束后正常退出游戏，原 Mods 归位，32 个原存档文件及 Options.ini、config.json 哈希一致。工具仍未接入运行时和公共 API，没有制作 ZIP 或改变游戏脚本源码。

### 短版回顾与引用验证

2026-09-18 新增 `experience_recap_v1`，从同一完整日志重新组织，再生成默认短版及独立详情 bundle。使用 `tiktoken 0.12.0 / o200k_base` 实测紧凑 JSON，包含短版范围说明、实体、引用、关系变化和待核查提示，不含详情与下游提示词。

| 人物 | 原 consumer_packet token | 短版 JSON token | 可读 Markdown token |
| --- | ---: | ---: | ---: |
| Eddie | 52,664 | 7,921 | 7,450 |
| Ava | 59,290 | 8,526 | 7,908 |
| Nyssa | 57,265 | 8,258 | 7,760 |
| Lila | 55,154 | 8,069 | 7,581 |

默认输入减少约 85%。当前保留全部 62／85／71／79 个已组织活动，包含尝试与依赖上下文；不是按硬上限截断。重要结果 16／17／15／16 项和状态区间 56／40／45／30 个仍呈现。关系数值汇总直接观测中的上升／下降次数，不推算净变化。部分人物略超出 8 千的试验目标，下一步依据阅读反馈调整常规社交和尝试的密度。

新增 14 项专项测试，覆盖来源与输出修改检测、混合 session、错误人物快照、原日志缺失或修订变化、查询分页、未开始与取消、未知行动、外部内容边界和结果呈现。全量 304 项通过。沿用的 33 项固定经历／知识检查、10 项结果及产物关联通过；Eddie 人工材料全部 75 个独立证据（含可选隐藏上下文）能通过分页原始查询回查。上一轮实机日志另作为边界样本复核，不当作本轮新拍的画面。

这些是固定样例与程序一致性检查。四人物短版已提供阅读样例，主观重要性和噪音程度仍待用户反馈；没有据此声明总体经历召回率或完成 Overlay 接入。

同日新实机 session `30a1f18e204c4a01a7967d8fce988288` 冻结了 535 个最新事件，序号 1,208 已全部落盘。10 项新检查通过：Lila 吉他 07:49–08:19 实际演奏后主动取消；研究请求在暂停中提交并取消、始终没有开始记录；Eddie 第一次电影 07:46–07:51 结束，第二次 08:29 开始且未见结束；Ava 07:44 开始的电影仍未结束，两人使用同一电视却保持独立活动。最终画面中 Lila 也坐在沙发上，但她的日志是聊天，不凭同座补写观影。

完整运行日志通过完整性检查，无采集／持久化错误、无淘汰。本轮记录了 5 张离散观察画面，没有连续录像。退出游戏后恢复原环境，32 个原存档的文件集合与哈希、Options.ini 和 config.json 哈希一致，临时测试目录已清理；通过安装器部署并核验当前源码对应的脚本包。新实机结果与旧实机回归分开保存，不将受控样例推广为全玩法结论。

同日依据 Eddie 阅读反馈迭代至 `experience_recap_v1_1`，以上 token 表保留为 v1 基线。r18／r19 是不同人物的独立排队尝试；名称质量元数据在压缩中保留，未知名称有统一回退及可回查来源；r54 恢复展示 19:25:29 入队与 19:33:50 开始的区别。增加 10 项回归测试，Python 3.7 全量 314 项通过；四人物全部原有活动、执行边界、角色、结果关联和证据集合一致。默认 JSON token 分别为 8,992／9,807／9,427／9,262，增量来自队列与名称质量信息。两轮已有实机日志均重新生成并通过原有 9／10 项检查，本次没有新开游戏。详细证据与限制见[debug 记录](experience-recap-debug.md)。
