# 项目协作约定

## 构建、安装与分发

用户于 2026-09-16 明确要求：以后每次修改代码后，不需要默认在项目目录中生成安装包；同日进一步授权，每次小修复完成后自动安装到本地。

- 日常代码修改完成后，执行与改动相称的检查和验证；不要自动运行 `scripts/package.py`，也不要自动新增 ZIP 分发包。
- 只有用户明确要求制作安装包或分发包时，才执行分发打包。
- 小范围修复通过相应检查后，自动完成本地安装；这是用户的持续授权，无需每次再询问。纯文档修改不需要重新安装。
- 本地安装前校验当前源码与构建 manifest 是否一致；必要时构建游戏需要的 `ContextOverlay.ts4script`，然后通过现有安装器直接部署，不必先生成 ZIP。
- 本地安装使用现有安装器的游戏进程检查、旧版备份及哈希校验。游戏运行时不替换脚本包，也不擅自关闭游戏；说明需要先退出游戏，再继续安装。
