# 游戏暂停时读取 0.7.1 实机数据

2026-09-16。用户保持游戏打开并暂停，检查期间 `TS4_x64` 进程仍在运行。**无需退出游戏，已能读取落盘日志和已完成的 Context 导出；本局 Autonomy 已产生 1,162 条决策，初始化修复得到实机验证。** 本次只读取数据并更新文档，没有操作游戏、修改运行时代码或重新安装。

## 快照范围

运行 ID：`9ba4280cb22d4c65b469cad11439a0c7`，日志确认加载 0.7.1。北京时间 15:33:17 读取固定长度的文件副本，读取前后文件大小及修改时间一致，未包含半行数据。分析的是这一时点已落盘的数据，不是对游戏内存的实时查询。

| 指标 | 当前快照 |
| --- | ---: |
| journal 大小 | 93,520,604 字节 |
| 完整日志行数／最终序号 | 17,909 |
| 事件数（取最新修订） | 8,057 |
| 交互事件 | 2,518 |
| Autonomy 决策 | 1,162 |
| 与决策相互关联的交互 | 1,162 |
| Context 导出 | Nyssa，`complete`，50 条近期历史 |

日志序列、会话和修订校验均通过。最后一条日志的记录时间为 15:30:48；没有 `session_end`，与本次游戏尚未退出一致，不构成损坏。Nyssa 的 Context 读取时间是 15:30:40，游戏时间第 3 天 07:03:33；其中身份、位置、时间、交互、需求、Buff、关系均可用。历史的 `truncated=true` 对应查询只返回近期 50 条，不能解释为原始日志丢失。

导出时记录器无错误、无 FIFO 淘汰，但后台当时还有 144 个排队任务。当前日志已完整到达 17,909，超过导出时已接受序号 17,904，说明这些日志随后继续写入。不能把导出中较早的队列状态当作暂停时的实时状态，也不能仅凭文件稳定推定所有内存数据均已落盘。

完整统计、原始样例及快照哈希见[机器可读报告](2026-09-16-autonomy-paused-live.json)。快照 SHA-256 为 `5821293735bf7b1d825c313b44004faca23508850b5eeec484f161013b61a0ac`。

## Autonomy 检查

开始观测显示 `enabled=true`、`coverage.state=installed`、`last_error=null`。1,162 个具体行为选择阶段均记录到 `roll_source=native_getter`，证明延迟接入的实际 Sim 类型入口已在本局工作；不是只根据启动标记判断成功。

- 共 2,886 层选择：862 层子行为提供者、862 层组别、1,162 层具体行为。
- 全部检查通过：每层最多五项、恰好保留一个赢家、保留排名符合“前五或最高四项加赢家”、候选概率与遗漏概率合计为一、决策与交互互相引用。
- 15 层的赢家原排名在前五之外，仍按约定保留；最大观测候选池为 41 项。
- 1,162 条决策全部经过成功入队门槛；429 条带实际缓存校验标记。
- 关联行为截至快照为完成 830、取消 320、失败 7、未知 5。其中 15 条成功入队但未观测到开始就被取消，决策仍保留。

按当前程序分层，主层为 300 条、内部层为 862 条。主层仍包含技术条目，这里只报告记录结果，不据此验收分层质量。1,613 条交互来源标为 Autonomy，但来源标记不等于独立、成功提交的决策，不能用它直接计算新增决策的覆盖率。

本快照没有保留下来的立即执行决策，也没有单独的目标选择层；这些边界仍需对应场景验证。会话未结束，尚无最终缓冲峰值、丢弃计数和关闭诊断；未进行受控性能对比。

## 一条真实选择

游戏第 2 天 **10:12:25**，Nyssa 在以下候选中选中了“扶起来”，目标是滚轮垃圾桶，随后成功入队并完成：

| 候选行为 | 原选择概率 | 结果 |
| --- | ---: | --- |
| 游玩（棋桌） | 25.10% | 未选中 |
| 浏览书籍 | 24.77% | 未选中 |
| 读点东西 | 24.77% | 未选中 |
| 丢掉（垃圾堆） | 13.96% | 未选中 |
| 扶起来（滚轮垃圾桶） | 11.41% | **选中并完成** |

这些概率来自本层完整五项选择池，展示百分比经过四舍五入。此例说明实际抽取可以选中权重较低的候选；不能只用最高分反推最终行为。具体评分证据、多任务检查及对应交互均随样例保存在报告 JSON 中。
